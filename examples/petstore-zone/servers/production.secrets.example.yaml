# Example secrets for the production environment.
# Copy this file to production.secrets.yaml (gitignored) and fill in credentials.
vars:
  admin_user: prod-admin
  admin_password: prod-super-secret-password
