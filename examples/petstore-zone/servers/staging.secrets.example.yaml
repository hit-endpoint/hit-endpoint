# Example secrets for the staging environment.
# Copy this file to staging.secrets.yaml (gitignored) and customize credentials.
vars:
  admin_user: staging_admin
  admin_password: staging_secret_password
